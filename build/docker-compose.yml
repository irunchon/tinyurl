version: "3.1"
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: urls_db
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5432:5432"